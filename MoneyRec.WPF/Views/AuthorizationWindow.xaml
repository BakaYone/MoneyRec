<Window x:Class="MoneyRec.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MoneyRec.WPF.Views"
        mc:Ignorable="d"
        Title="Авторизация" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height=".1*"/>
        </Grid.RowDefinitions>
        <Frame Grid.Row="0" Content="{Binding CurrentInstance}" NavigationUIVisibility="Hidden"/>
        <Button Grid.Row="1" Content="{Binding ButtonName}" Command="{Binding ChangeInstanceCommand}" Height="25" Width="120" HorizontalAlignment="Left" Margin="10,0,0,0"/>
    </Grid>
</Window>
